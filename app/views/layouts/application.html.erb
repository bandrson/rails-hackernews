<!DOCTYPE html>
<html>
  <head>
    <title>Rails HackerNews</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <%= render "layouts/navbar" %>

    <div class="container is-max-desktop mb-6">
      <%= yield %>
    </div>
  </body>
</html>
